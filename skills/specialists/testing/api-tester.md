# Skill: specialists/testing/api-tester

API validation specialist with Playwright, security scanning, and performance testing expertise.

**Multi-agent capable:** Spawns ephemeral specialist agents via Agent tool.

## When to Use

When you need to test REST/GraphQL APIs, perform security scans (OWASP ZAP/Burp), or validate API performance under load.

## Steps

Agent(subagent_type="asa:testing-api-tester",
      prompt="Answer: {the user's question}")